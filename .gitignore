# SwiftPM
.build/
.swiftpm/
Package.resolved

# Packaging output (built .app + zip live only in GitHub Releases, never in git)
dist/

# Stray compiler artifacts (sometimes dropped in the repo root by a failed link)
*.o
*.d
*.swiftdeps
*.swiftdeps~

# macOS
.DS_Store

# Editors
*.swp
.vscode/
.idea/
